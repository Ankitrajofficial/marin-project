# OPTIONAL. The app runs fully without this key (rule-based planner + template synthesis).
# With it set, planner.ts and synthesis.ts use Claude for intent parsing and answer generation.
ANTHROPIC_API_KEY=
