# The venv is the big one: ~1 GB of macOS-native wheels that are useless in a
# linux image and would be reinstalled by pip anyway.
backend/.venv/
frontend/
**/__pycache__/
**/*.pyc
.git/
.env
**/.env
data/
*.nc
